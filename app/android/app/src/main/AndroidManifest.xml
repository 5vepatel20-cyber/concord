<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ── Concord day-1 permissions ──────────────────────────────────
         Each one is required for a feature shipping in 1.0/1.1.
         Declared up front so re-builds in CI don't keep re-adding them. -->

    <!-- Daily check-in notification (SYM-03). Android 13+ requires the
         runtime permission; the Flutter layer prompts for it. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Schedule the daily check-in at the user-chosen wall-clock time.
         USE_EXACT_ALARM is the Android 14+ way; SCHEDULE_EXACT_ALARM is
         the Android 12/13 fallback. flutter_local_notifications picks. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>

    <!-- Reschedule notifications after a device reboot so the daily
         check-in doesn't silently die. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- Health Connect is the Android analog of HealthKit (HK-01).
         Google fit permissions were deprecated; Health Connect is the
         supported path as of Android 14. -->
    <uses-permission android:name="android.permission.health.READ_STEPS"/>
    <uses-permission android:name="android.permission.health.READ_HEART_RATE"/>
    <uses-permission android:name="android.permission.health.READ_SLEEP"/>
    <uses-permission android:name="android.permission.health.READ_WEIGHT"/>

    <!-- Internet is already implicit but spelled out so it's obvious
         the app needs network for Supabase + Atlas. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Voice input for free-text notes (SYM-05). RECORD_AUDIO is the
         runtime permission for the mic; BLUETOOTH_CONNECT lets the
         speech recognizer route through a paired headset mic. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>

    <application
        android:label="concord"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Custom URL scheme deep-link (matches iOS com.concord.app).
                 concord://log opens the quick-log; concord://report/<id>
                 jumps to a specific report. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="com.concord.app"/>
            </intent-filter>
        </activity>

        <receiver
            android:name=".SymptomWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/symptom_widget_info" />
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Android 11+ package visibility: speech_to_text needs to
             discover a RecognitionService to bind to. Without this, the
             listen call fails silently with no recognizer available. -->
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
    </queries>
</manifest>